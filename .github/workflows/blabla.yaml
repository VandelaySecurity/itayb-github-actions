name: blabla
on:
  push:
    branches:
      - main
  pull_request:
jobs:
  blabla:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Run Tests
        run: echo "Running tests..."
